--extra-index-url https://download.pytorch.org/whl/cu128
# NOTE: PyTorch is installed separately in the Dockerfile before installing
# the rest of the requirements because some packages (e.g. openfold) need
# `torch` available at build time.

# pip-only dependencies from environment.yml
e3nn==0.5.1
PyYAML==6.0.1
rdkit==2023.3.3
pandas==2.0.3
biopython==1.81
networkx==3.2.1
spyrmsd==0.6.0
scipy==1.10.1
fair-esm[esmfold]==2.0.0
# Note: dllogger and openfold are installed from git AFTER torch is present
# (they are installed in the Dockerfile). If pip installation fails for
# binary packages like rdkit or some torch-geometric subpackages, consider
# switching back to conda or providing prebuilt wheels in the build context.
